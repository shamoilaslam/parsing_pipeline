# Input data

Put source PDFs in `data/pdfs/`. The parser reads them but never modifies them.

Reference JSONs belong in `data/references/llamaparse/`. They are comparison artifacts, not verified ground truth. Human-verified annotations should use a separate `data/references/gold/` directory.
