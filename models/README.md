# Local models

Optional local OCR models live here. Model caches are ignored by Git. The digital native parser does not require a model.

The current `rapidocr_arabic/` model is used only by the local scanned/Urdu OCR path when explicitly enabled.
